# Django + React Full-Stack App

Full-stack application with a Django REST Framework backend and React frontend.

## Structure
- `backend/` — Django REST API
- `frontend/` — React app
- `frontend1/` — alternate frontend version

## Setup
```bash
# backend
cd backend && pip install -r requirements.txt && python manage.py runserver
# frontend
cd frontend && npm install && npm start
```
